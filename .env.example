# Required
UNISCRIBE_API_KEY=your_api_key_here

# Optional: port for the n8n webhook listener
PORT=3000

# Optional: if set, /render and /jobs require this key via X-API-Key header
WEBHOOK_API_KEY=generate_a_long_random_string
